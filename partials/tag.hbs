<div class="article_tags">
    {{#foreach tags limit="1"}}
        <a href={{url}}>
            <span style="--color-accent: {{accent_color}}" class="tag">{{name}}</span>
        </a>
    {{/foreach}}

</div>
